import styled from 'styled-components';

const Card = styled.div`
  display: inline-flex;
  align-items: center;
  flex-direction: column;
  background-color: var(--main-700);
  padding: 20px 40px;
  border-radius: 10px;
  box-shadow: 0 0 10px var(--main-700);
  margin-left: auto;
  margin-right: auto;
`;

export default Card;